defaults:
  - model: spikeformer
  - dataset: default
  - split: default
  - _self_

num_epochs: 50
batch_size: 64
learning_rate: 0.0001
weight_decay: 0.0
output_dir: artifacts/train
device: auto
num_workers: 0
full_episode_eval:
  enabled: true
  every_n_epochs: 5
  num_episodes: 1
  save_visualizations: true
